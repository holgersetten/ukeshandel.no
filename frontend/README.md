# Frontend

React, TypeScript og Vite. Kjør `npm install` og `npm run dev` i denne mappen. `npm run build` kontrollerer TypeScript og bygger appen.

Se [prosjektets README](../README.md) for oppstart av backend, dataflyt og prosjektstruktur.
